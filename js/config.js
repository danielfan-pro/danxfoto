/* ═══════════════════════════════════════
   config.js — DanXFoto
   Obfuscated credentials
   ⚠️ Add this file to .gitignore
═══════════════════════════════════════ */

const _cfg = (() => {
  // Keys are split + reversed to avoid plain-text exposure
  const _a = (s) => s.split('').reverse().join('');
  const _b = (parts) => parts.join('');

  return {
    pk: _a(_b(['HAY','sJD','BYd','Ehe','MQ7','q8'])),
    si: _a(_b(['ila','p7f','d_e','civ','res'])),
    ti: _a(_b(['fc2','ub8','4_e','tal','pme','t'])),
  };
})();
